using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Manager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    
    //position the camera
    //set the pic per angle
    //when press space
        //camera rotate around the building
        //takes pictures at the pic per angle rate
        //save images into image array
        //when finish - save image array as an image file


    }

    
}
